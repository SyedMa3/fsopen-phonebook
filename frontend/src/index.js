import React from 'react';
import ReactDOM from 'react-dom/client';
import axios from 'axios'
import App from './App';
const root = ReactDOM.createRoot(document.getElementById('root'));

// cosnt promise = axios.get()

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

